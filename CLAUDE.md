# כללי עבודה בפרויקט

## סגנון כתיבה

**מקפים.** משתמשים רק במקף רגיל `-`. אין להשתמש במקף ארוך (em dash) או במקף בינוני (en dash). הכלל חל על תוכן האתר, על הודעות הקומיט, ועל התכתובת בצ'אט.

יוצא דופן אחד: המקף העברי (maqaf, `־`) נשאר במקומות שבהם הוא חלק מהאיות התקני, למשל "גופנית־נפשית", "גוף־נפש", "התשע"ג־2013".

**קול.** עברית בגובה העיניים, פנייה בלשון נקבה, בלי סופרלטיבים ובלי שפה שיווקית. הטקסטים באתר נכתבו בקול של אנה - שומרים עליו.

## מבנה

- `index.html` - דף הבית. רכיב x-dc יחיד, הלוגיקה בתחתית הקובץ.
- `support.js` - ספריית הרנדור של הרכיב. לא נוגעים.
- `articles/` - חמישה מאמרים כעמודים עצמאיים.
- `accessibility.html` - הצהרת נגישות. `thanks.html` - עמוד תודה לטופס.
- `illustrations/` - איורי אקוורל ב-WebP.

**התוכן מרונדר מראש.** לולאות `sc-for` הורחבו ל-HTML סטטי כדי שגוגל תראה תוכן בלי להריץ JS. מערכי הנתונים ב-`renderVals` נשארו כקוד מת. **כשמשנים טקסט שמופיע גם במערך וגם ב-HTML - צריך לעדכן את שניהם.**

## עריכה ופרסום

עורכים ישירות בקבצים, עדיף בהחלפה מדויקת ולא בשכתוב קובץ שלם.

לפני קומיט מריצים בדיקה שהראנטיים לא נשבר:

```js
// טוען את הרכיב ומריץ renderVals
const m = fs.readFileSync('index.html','utf8').match(/<script type="text\/x-dc"[^>]*>([\s\S]*?)<\/script>/);
```

**פרסום: לא דוחפים ל-`main`.**

התוכנית החינמית בנטליפיי נותנת 300 קרדיטים לחודש, וכל דיפלוי עולה כ-15 - כלומר **כעשרים דיפלויים בחודש לכל הטים**. הטים משותף עם דף הנחיתה `red-detox`, אז דחיפה כאן גורעת גם משם. משך הבנייה לא משנה.

ההגדרה היא `Deploy only the production branch`, ולכן ענף עבודה לא עולה כלום:

```bash
git checkout -b edits            # ענף עבודה
git push origin edits            # אפס קרדיטים
# רק כשהכל מוכן ומאומת:
git checkout main && git merge edits && git push   # דיפלוי אחד
```

**אין לפתוח Pull Request** - הוא מייצר Deploy Preview שלא אומת כפטור.

מחזור החיוב מתאפס ב-15 בכל חודש. נטליפיי בונה תוך 30-60 שניות.

אימות שהשינוי עלה: `curl -s https://anna-meyuhas.com | grep <מחרוזת>`

## שירותים מחוברים

- **אחסון:** Netlify, פרויקט `anna-meyuhas`, מחובר לגיטהאב
- **טפסים:** Netlify Forms - `contact` ו-`waitlist`, התראות למייל
- **יומן:** Calendly - https://calendly.com/meyuhastv/30min
- **דומיין:** Porkbun, ALIAS ל-apex-loadbalancer.netlify.com
- **קליניקה:** מרכז העסקים ב.ס.ר סיטי, ז'בוטינסקי 61 פתח תקווה. קואורדינטות במפה: 32.0918374, 34.8600663

## מלכודות שכבר נתקלנו בהן

- **הרנדרר משמיט את התכונה `hidden`.** להסתרה משתמשים ב-`style` מוטבע.
- **אין להחזיק את הריפו בדרופבוקס.** הסנכרון נועל את `.git` ושובר פעולות.
- **מיקוד בשאילתת מפה מסיט את הסיכה.** משתמשים בקואורדינטות מפורשות.
- **בדיקה של ה-HTML הגולמי לא מספיקה.** יש הבדל בין המקור למה שהרנדרר מייצר בפועל - כדאי לאמת גם בדפדפן, וגם בנייד.
